# Lifecycle and Ownership Matrix — Phase 0 HAL-Interface Baseline

Evidence from pokeysHal@cd1f0dc8a0f64f92dc6bdce21bddcb36d33a14cd.

---

## A. pokeysHal Component Lifecycle and Ownership

### Column Definitions

- **HAL storage**: where HAL-visible memory is allocated
- **Allocation**: which function calls `hal_malloc`
- **Object creation**: which function calls `hal_pin_*_newf` / `hal_param_*_newf`
- **Initial defaults**: where initial values are written into the HAL objects
- **Device→HAL update**: which function reads device state and writes to HAL pins
- **HAL→device update**: which function reads HAL pins and writes to device
- **Physical configuration**: which function reads/writes `PinFunction` on device
- **Readiness dependency**: whether `hal_ready` must be called before this step
- **Cleanup**: whether any cleanup occurs on `hal_exit`
- **Evidence**: file:line for each claim

---

| Subsystem | HAL storage | Allocation | Object creation | Initial defaults | Device→HAL update | HAL→device update | Physical configuration | Readiness dependency | Cleanup | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|
| **Lifecycle (component)** | `struct __comp_state` via `hal_malloc` | `experimental/pokeys_async.c:91` (`export()`) | not applicable | all pin pointers zeroed by `memset(inst, 0, sz)` | n/a | n/a | n/a | `hal_ready(comp_id)` at line 210 after all exports succeed | `hal_exit(comp_id)` at lines 208, 216 | `experimental/pokeys_async.c:86-148` |
| **Component pins** (devSerial, alive, err, in[], ain[]) | within `__comp_state` (auto-generated by halcompile) | `hal_malloc(sz)` in `export()` | auto-generated code from `.comp` MODULE_INFO | zeroed by `memset` | RT function `_()` sets `alive`, error handler sets `err` | RT function reads `devSerial` | n/a | before `hal_ready` | part of component cleanup | `experimental/pokeys_async.c:47-61; experimental/pokeys_async.comp:5-9` |
| **Physical PinFunction** | `device->Pins[j].PinFunction` (`hal_u32_t`) via `hal_malloc` in `PK_CreateDeviceEx` | `PoKeysLibCoreAsync.c:128` | `PoKeysLibIOAsync.c:26` (`hal_param_u32_newf`) | zeroed by `memset` in `PK_CreateDeviceEx` | `PK_ParsePinFunctionsResponse` (PoKeysLibIOAsync.c:143) | `PK_PinFunctionsSetAsync` (PoKeysLibIOAsync.c, around line 333) | `PK_StartPinFunctionsRequestAsync` reads; `PK_PinFunctionsSetAsync` writes | before `hal_ready` | none found | `PoKeysLibIOAsync.c:7-141; PoKeysLibCoreAsync.c:128` |
| **Digital Input** (digin) | `device->Pins[j].DigitalValueGet` (`hal_digin_t`) via `hal_malloc` | `PoKeysLibCoreAsync.c:128` | `PoKeysLibIOAsync.c:35` via `hal_export_digin` | zeroed by `memset` | `PK_DigitalIOGetParse` (PoKeysLibIOAsync.c:~479) | not applicable (HAL_OUT pins) | `PK_StartPinFunctionsRequestAsync` sets PinFunction; `PK_PinFunctionsSetAsync` applies invert | before `hal_ready` | none found | `PoKeysLibIOAsync.c:35,43; hal-canon/hal_digital.c` |
| **Digital Output** (digout) | `device->Pins[j].DigitalValueSet` (`hal_digout_t`) via `hal_malloc` | `PoKeysLibCoreAsync.c:128` | `PoKeysLibIOAsync.c:53` via `hal_export_digout` | zeroed by `memset` | not applicable (HAL_IN pin) | `PK_DigitalIOSetAsync` (PoKeysLibIOAsync.c:~385-465) | `PK_PinFunctionsSetAsync` propagates `invert` to `PinFunction` | before `hal_ready` | none found | `PoKeysLibIOAsync.c:53; PoKeysLibIOAsync.c:385-465` |
| **Analog Input** (adcin, non-canonical) | `device->Pins[40+j].AnalogValue` (pointer) and `device->AnalogInput[j]` (`sPoKeysAnalogData`) via `hal_malloc` | `PoKeysLibCoreAsync.c:136` | `PoKeysLibIOAsync.c:76,83,90` | zeroed by `memset` | `PK_AnalogIOParse` (PoKeysLibIOAsync.c:~internal) | not applicable | none | before `hal_ready` | none found | `PoKeysLibIOAsync.c:76-90; PoKeysLibCoreAsync.c:136` |
| **Analog Input Canon** (adcin via hal_export_adcin) | `device->AnalogInput[j].Canon` (`hal_adcin_t`) via `hal_malloc` | `PoKeysLibCoreAsync.c:136` | IMPLEMENTED via `hal_export_adcin` (PoKeysLibIOAsync.c:85). Direction bug: `adcin.value` exported as HAL_IN (should be HAL_OUT). See CONFLICT-009. | `Canon.scale` and `Canon.offset` defaults unknown | `PK_AnalogIOParse` computes `Canon.value` internally | n/a | n/a | n/a | n/a | `PoKeysLibHal.h:740; PoKeysLibIOAsync.c (PK_AnalogIOParse)` |
| **Analog Output** (adcout, non-canonical) | `device->PWM.PWMduty[j]` (pointer) and `device->PWM.max_Voltage[j]` via `hal_malloc` | `PoKeysLibCoreAsync.c:157,161,167` | `PoKeysLibIOAsync.c:117,124,132` | zeroed by `memset` | `PoKeysLibIOAsync.c:124` (`PWMduty` reads from device) | `PK_PWMAsync` sends duty/period | none | before `hal_ready` | none found | `PoKeysLibIOAsync.c:117-132; PoKeysLibCoreAsync.c:157` |
| **Analog Output Canon + PWM** (adcout via hal_export_adcout + PK_PWMUpdateAsync) | `device->PWM.PWManalogOutputs[j]` (`hal_adcout_t`) via `hal_malloc` | `PoKeysLibCoreAsync.c:618` | IMPLEMENTED via `hal_export_adcout` (PoKeysLibIOAsync.c:110). Conversion: adcout.J.value → hal_adcout_getscaledvalue → PWMduty → CMD 0xCB. | 0.0 (memset) | n/a | `PK_PWMUpdateAsync` reads `PWManalogOutputs[j].value`, converts to duty (PoKeysLibIOAsync.c:703–734) | n/a | Scheduler invokes `PK_PWMUpdateAsync` at 100 ms (experimental/pokeys_async.c:1338) | `hal_exit(comp_id)` | `PoKeysLibIOAsync.c:703; experimental/pokeys_async.c:1338; experimental/Submakefile.rt (F-009)` |
| **Encoders** (basic) | `device->Encoders[i]` (`sPoKeysEncoder`) via `hal_malloc` | `PoKeysLibCoreAsync.c:147` | `PoKeysLibEncodersAsync.c:30` (`export_encoder_pins`) | zeroed by `memset` | `PK_EncoderValuesGetAsync` / `PK_EncoderValuesGetParse` | `reset`, `index-enable` read from HAL in update function | none (encoder pins are fixed, no PinFunction config) | before `hal_ready` | none found | `PoKeysLibEncodersAsync.c:30-108; PoKeysLibCoreAsync.c:147` |
| **Fast Encoders** | `device->FastEncoders*` fields in `sPoKeysDevice` | part of device struct | `PoKeysLibEncodersAsync.c:111-150` | zeroed by `memset` | `PK_FastEncodersGetAsync` (not inspected) | write back via params | `PK_FastEncoderConfigurationSet` | before `hal_ready` | none found | `PoKeysLibEncodersAsync.c:111-150` |
| **UltraFast Encoder** | `device->UltraFastEncoder*` fields in `sPoKeysDevice` | part of device struct | `PoKeysLibEncodersAsync.c:155-167` | zeroed by `memset` | `PK_UltraFastEncoderGetAsync` (not inspected) | write back via params | device config | before `hal_ready` | none found | `PoKeysLibEncodersAsync.c:155-167` |
| **RTC** | `device->RTC` (`sPoKeysRTC`) with pointer fields via `hal_malloc` | part of device struct; pointers set in `export_rtc_pins` | `PoKeysLibRTCAsync.c:56` (`export_rtc_pins`) | zeroed | `PK_RTCGetAsync` / parse function | not applicable (all HAL_OUT) | none | before `hal_ready` | none found | `PoKeysLibRTCAsync.c:56-90` |
| **PoNET** | `device->PoNETmodule` with pointer fields | `PoKeysLibPoNETHal.c` | `PoKeysLibPoNETHal.c:93-221` (`export_ponet_pins`) | zeroed | PoNET status read functions | `PoNETmodule.statusOut_pins` | none | before `hal_ready` | none found | `PoKeysLibPoNETHal.c:93-221` |
| **PEv2 global** | `device->PEv2` (`sPoKeysPEv2HAL`) with pointer fields | struct is part of device | `PoKeysLibPulseEngine_v2Async.c:373` (`export_pev2_pins`) | zeroed by `memset` | `PK_PEv2_StatusGetAsync` / `PK_PEv2_StatusUpdateHALAsync` | `PK_PEv2_MovePVFromHALAsync`, `PK_PEv2_ExternalOutputsFromHALAsync` | `PK_PEv2_AxisConfigurationGet/SetAsync` | before `hal_ready` | none found | `PoKeysLibPulseEngine_v2Async.c:373-715; experimental/pokeys_async.c:129` |
| **PulseEngineState (device→HAL)** | `device->PEv2.PulseEngineState` (u32) | part of device struct | `export_pev2_pins()` creates `pin_PulseEngineState` (PoKeysLibPulseEngine_v2Async.c:470) | 0 | `PK_PEv2_StatusUpdateHALAsync` parse: `*pev2->pin_PulseEngineState = pev2->PulseEngineState` (line 796) | not applicable (HAL_OUT) | n/a | before `hal_ready` | `hal_exit(comp_id)` | `PoKeysLibPulseEngine_v2Async.c:470,796` |
| **PEv2 per-axis** | arrays within `device->PEv2`: `pin_joint_pos_cmd[8]` etc. | struct arrays within device | `PoKeysLibPulseEngine_v2Async.c:380` (loop `i=0..7`, unconditional) | zeroed by `memset` | `PK_PEv2_StatusGetAsync` reads position, state; parsed into `pin_joint_pos_fb`, `pin_CurrentPosition`, `pin_AxesState` | `PK_PEv2_PulseEngineMovePVAsync` reads `pin_joint_pos_cmd`, `pin_joint_vel_cmd` | `PK_PEv2_AxisConfigurationGet/SetAsync` | before `hal_ready` | none found | `PoKeysLibPulseEngine_v2Async.c:380-435` |
| **PoExtBus** | `device->PoExtBusData` via `hal_malloc` | `PoKeysLibCoreAsync.c:172` | **ABSENT** — no `hal_pin_*_newf` calls | n/a | n/a | n/a | n/a | n/a | n/a | `PoKeysLibCoreAsync.c:172; issue #34` |

### Key Observations (pokeysHal component)

1. **All HAL memory is allocated via `hal_malloc`**, which is correct per A-001.
   There are no `malloc`/`calloc` calls for HAL-visible structures.
   `hal_free` is correctly absent throughout the codebase.

2. **All object creation occurs in `export()` → `export_*_pins()`**, before
   `hal_ready(comp_id)` is called at `experimental/pokeys_async.c:210`.
   This satisfies the LinuxCNC lifecycle requirement.

3. **The integration shell (`experimental/pokeys_async.c`) is the sole lifecycle
   owner**: it calls `hal_init`, all export functions, `hal_export_funct`,
   `hal_ready`, and `hal_exit`. Subsystem files own their own object creation.

4. **Initial defaults**: The consumer component's own pin pointers in `__comp_state`
   are zeroed by `memset(inst, 0, sz)` in `export()` (F-008, inspected). Non-pointer HAL
   params in library structs (e.g., `encoder.scale`) are also zeroed via `memset` in
   `PK_CreateDeviceEx`. Zeroing of the broader RTAPI shared-memory region is implementation-
   dependent and has no registered primary source in Phase 0; do not rely on it for defaults.

5. **Cleanup** is handled by `hal_exit(comp_id)`. All HAL-malloc memory is
   reclaimed at exit. Per-subsystem cleanup is component-level.

6. **PoExtBus** has allocated storage (`hal_malloc` at `PoKeysLibCoreAsync.c:172`)
   but no HAL pin export — the most significant remaining ownership gap.
   **Canonical adcin and adcout** are exported via `hal_export_adcin` and
   `hal_export_adcout` respectively. The adcin direction bug (value as HAL_IN)
   is a hal-canon implementation defect inherited by all callers (CONFLICT-009).

---

## B. External Counterpart Evidence: pokeys_homecomp

**Source:** E-010 (`pokeys_rt/pokeys_homecomp.comp` in `zarfld/LinuxCnc_PokeysLibComp@0c058e6c`)

`pokeys_homecomp` is a **separate, independently loaded LinuxCNC component**.
It is not a subsystem of pokeysHal.
It is not implemented by pokeysHal.
It owns the `joint.N.*` pins exclusively.
Its lifecycle (hal_init, hal_ready, hal_exit) is external to pokeysHal and
managed by the LinuxCNC runtime.

| Property | Value |
|---|---|
| Component | `pokeys_homecomp` |
| Repository | `zarfld/LinuxCnc_PokeysLibComp` (E-010) |
| Pins owned | `joint.N.home-sw-in`, `joint.N.homing`, `joint.N.homed`, `joint.N.home-state`, `joint.N.index-enable`, `joint.N.PEv2.AxesState`, `joint.N.PEv2.AxesCommand`, etc. |
| HAL memory | `pokeys_homecomp` allocates its own HAL objects through the LinuxCNC RTAPI path described in E-010. The Phase 0 evidence set does not claim any additional pokeysHal-specific initialization semantics for this external component. |
| Initialization | `homing_init()` calls `makepins()` at line 364, then returns. The explicit init loop at lines 366–397 is unreachable dead code. The startup semantics for the external pins are therefore documented from E-010 only; no pokeysHal F-class evidence is claimed for this section. |
| Internal defects | The counterpart `pokeys_homecomp` (E-010) has an unreachable initialization block in `homing_init()`. This is internal to `zarfld/LinuxCnc_PokeysLibComp` and is not a pokeysHal library issue. See the out-of-scope note in `conflict-register.md`. |
| Lifecycle owner | LinuxCNC runtime (loads/unloads `pokeys_homecomp` independently) |

---

## C. Integration Lifecycle

The end-to-end system requires both components to be loaded and connected through
HAL configuration. Neither component creates or owns the other's pins.

| Aspect | Description |
|---|---|
| Load requirement | Both `pokeys_async` (or legacy pokeys component) and `pokeys_homecomp` must be loaded |
| Pin ownership | Each component creates and owns its own pins; no shared ownership |
| Signal creation | HAL configuration (`net` statements in the machine HAL file) connects endpoints |
| Joint-to-axis mapping | Configuration-specific; not generated by pokeysHal |
| Load order | Both components must complete `hal_ready` before HAL signals can be fully connected |
| Integration catalogue | `integration-links.yaml` (IK-001..IK-004) is the authoritative link record |

Integration links IK-001..IK-004 record the producer/consumer endpoints, HAL
types, pin directions, semantic contracts, and known incompatibilities (IK-003:
CONFLICT-013, CONFLICT-014).

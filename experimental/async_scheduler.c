/* async_scheduler.c — backward-compatibility shim
 *
 * The async scheduler implementation has been migrated into PoKeysLibAsync.c.
 * This file intentionally contains no code so that it can be removed from the
 * build without any source-level breakage.  All symbols are now provided by
 * PoKeysLibAsync.c which is already compiled into libPoKeysHal.
 */
